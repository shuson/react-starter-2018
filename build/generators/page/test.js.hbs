import test from 'ava';
import React from 'react';
import {shallow} from 'enzyme'

import { {{ properCase name }} } from '../{{name}}';

test('{{ properCase name }} page container', (t) => {
  const wrapper = shallow(<Foo />);
	
});
