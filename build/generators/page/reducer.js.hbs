import { fromJS } from 'immutable';
import {
  DEFAULT_ACTION,
} from '../actions/{{name}}';

const initialState = fromJS({});

function {{ name }} (state = initialState, action) {
  switch (action.type) {
    case DEFAULT_ACTION:
      return {
        ...state,
      };
    default:
      return state;
  }
}

export default {{ name }};
